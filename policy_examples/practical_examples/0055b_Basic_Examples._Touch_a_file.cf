bundle agent example {

    files:

        "$(sys.workdir)/i_am_alive" 

            create  => "true",
            touch   => "true",
            comment => "Update timestamp on this file so my sysadmin knows I am alive and faithful.";
}
