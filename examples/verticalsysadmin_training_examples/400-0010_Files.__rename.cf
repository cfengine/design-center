body common control {

               inputs => { "/var/cfengine/inputs/libraries/cfengine_stdlib.cf" };

                    }


bundle agent example {

  files:
      "/bin/chown"
               rename => to("/bin/CHOWN");
                     }

