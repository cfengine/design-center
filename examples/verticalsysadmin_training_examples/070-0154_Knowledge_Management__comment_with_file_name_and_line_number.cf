bundle agent example {

  files:

      "/tmp/testfile" 


              comment => "/tmp/testfile must exist for application X to work.  This promise came from $(this.promise_filename).  It occurred on line number $(this.promise_linenumber).  The promiser is $(this.promiser).",
      # this comment will show up in verbose or debug modes

              create  => "true";
}
