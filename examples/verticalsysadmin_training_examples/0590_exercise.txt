EXERCISE

Create a list containing names of files to create.

Then use a single "files" promise to create all
these files.

----

If you finish before the rest of the class, additional
exercise:
1. create a base list of packages that should not
be on the system.
2. make another list of packages that should not
be on the system in a particular class 
3. assuming your system is in that particular class,
make sure both base and class-based black-listed packages
are absent.
