bundle agent example {

files:

  "/etc/motd"

    create => "true",
    edit_line => ExpandFromTemplate("templates/motd.txt");
}

bundle edit_line ExpandFromTemplate(source) {

delete_lines:

  ".*";

insert_lines:

    "$(source)"

       insert_type => "file",
       expand_scalars => "true";
}
