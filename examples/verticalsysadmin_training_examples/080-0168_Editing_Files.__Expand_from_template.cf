bundle agent example {

files:

  "/etc/motd"

    edit_line => InsertFile("/tmpl/motd.tmpl");
}

bundle edit_line InsertFile(source) {

insert_lines:

    "$(source)"

       insert_type => "file";
}
