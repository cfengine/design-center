bundle agent example {

    files:

        "/var/cfengine/i_am_alive" 

            create  => "true",   # promise body starts here
            touch   => "true",   # promise body
            comment => "Update timestamp on this file so my sysadmin knows I am alive and faithful.";    # promise body ends here
}
