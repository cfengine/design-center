bundle agent example {

  files:

      "/tmp/cfengine_heartbeat" -> "Monitoring System" 

               handle => "update_heartbeat",
              comment => "Update heartbeat timestamp to confirm CFEngine is running",
              create  => "true", 
              touch   => "true";
}
