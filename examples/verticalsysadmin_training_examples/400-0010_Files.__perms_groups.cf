bundle agent example {

  files:

      "/tmp/testfile"

                perms => acceptable_groups;
                     }

body perms acceptable_groups {

               groups => {"root", "games", "mail" };

                             }
