[
   // this is the install server bundle call
   { "Database::Install::MySQL": {server:true, purge: false} },

   // this is the purge bundle call
   { "Database::Install::MySQL": {server: true, purge:true} },
]