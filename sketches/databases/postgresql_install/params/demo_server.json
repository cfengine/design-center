[
   // this is the install server bundle call
   { "Database::Install::PostgreSQL": {server:true, purge: false} },

   // this is the purge bundle call
   { "Database::Install::PostgreSQL": {server: true, purge:true} },
]