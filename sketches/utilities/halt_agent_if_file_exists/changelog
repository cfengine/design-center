halt_agent_if_file_exists 1.0
  * Add bundle to act as single interface between alert and abort bundles
  * Change public api to cf-sketch style prefix
  * sketchify - prefix with cfdc, metadata
  * Add test
