set_hostname 1.05.2
  * Fix domainname setting.
set_hostname 1.03
  * fix error preventing install via cf-sketch
set_hostname 1.01
  * cf-sketchify - prefix with cfdc, add metadata, add test etc ...
  * switch to cfsketch prefix style API
  * Move out from unsupported
